---
- name: Find duplicate users and their detailed permissions
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    aap_hostname: "{{ lookup('env', 'CONTROLLER_HOST') }}"
    aap_username: "{{ lookup('env', 'CONTROLLER_USERNAME') }}"
    aap_password: "{{ lookup('env', 'CONTROLLER_PASSWORD') }}"
    aap_validate_certs: true

  tasks:
    - name: Load all users
      ansible.builtin.set_fact:
        all_users: "{{ query('ansible.platform.gateway_api', 'users', return_all=True, return_objects=True, host=aap_hostname, username=aap_username, password=aap_password, verify_ssl=aap_validate_certs) }}"

    - name: Group users by email
      ansible.builtin.set_fact:
        users_by_email: "{{ users_by_email | default({}) | combine({ item.email: (users_by_email[item.email] | default([])) + [item] }) }}"
      loop: "{{ all_users }}"
      when: item.email is defined and item.email | length > 0

    - name: Build primary map (first user = primary)
      ansible.builtin.set_fact:
        primary_map: "{{ primary_map | default({}) | combine({ item.key: item.value }) }}"
      loop: "{{ users_by_email | dict2items }}"
      when: item.value | length > 1

    # ---------------------------------------------------------
    # GET PRIMARY USER ROLE IDS (ROBUST)
    # ---------------------------------------------------------
    - name: Get primary user roles
      ansible.builtin.set_fact:
        primary_roles_map: "{{ primary_roles_map | default({}) | combine({ item.key: role_ids }) }}"
      vars:
        assignments: "{{ query('ansible.platform.gateway_api', 'role_user_assignments',
          query_params={'user_id': item.value[0].id},
          host=aap_hostname, username=aap_username,
          password=aap_password, verify_ssl=aap_validate_certs) }}"
        role_ids: >-
          {% set ids = [] %}
          {% for a in assignments %}
            {% set rid = a.role_definition.id if a.role_definition is mapping else a.role_definition %}
            {% if rid not in ids %}
              {% set _ = ids.append(rid) %}
            {% endif %}
          {% endfor %}
          {{ ids }}
      loop: "{{ primary_map | dict2items }}"

    # ---------------------------------------------------------
    # BUILD DUPLICATE USER + ROLE MATRIX
    # ---------------------------------------------------------
    - name: Build duplicate assignment matrix
      ansible.builtin.set_fact:
        dup_matrix: "{{ dup_matrix | default([]) + [ {
          'email': item.0.key,
          'primary_id': item.0.value[0].id,
          'dup_id': item.1.id,
          'assignments': query('ansible.platform.gateway_api', 'role_user_assignments',
            query_params={'user_id': item.1.id},
            host=aap_hostname, username=aap_username,
            password=aap_password, verify_ssl=aap_validate_certs)
        } ] }}"
      loop: "{{ primary_map | dict2items | subelements('value[1:]', skip_missing=True) }}"

    # ---------------------------------------------------------
    # MERGE ROLES (NO BLOCKS)
    # ---------------------------------------------------------
    - name: Assign missing roles to primary users
      ansible.builtin.uri:
        url: "{{ aap_hostname }}/api/gateway/v1/role_user_assignments/"
        method: POST
        user: "{{ aap_username }}"
        password: "{{ aap_password }}"
        force_basic_auth: true
        validate_certs: "{{ aap_validate_certs }}"
        body_format: json
        body:
          user: "{{ item.0.primary_id }}"
          role_definition: "{{ (item.1.role_definition.id if item.1.role_definition is mapping else item.1.role_definition) }}"
      loop: "{{ dup_matrix | subelements('assignments') }}"
      when: >
        (item.1.role_definition.id if item.1.role_definition is mapping else item.1.role_definition)
        not in primary_roles_map[item.0.email]

    # ---------------------------------------------------------
    # DELETE DUPLICATES
    # ---------------------------------------------------------
    - name: Delete duplicate users
      ansible.builtin.uri:
        url: "{{ aap_hostname }}/api/gateway/v1/users/{{ item.dup_id }}/"
        method: DELETE
        user: "{{ aap_username }}"
        password: "{{ aap_password }}"
        force_basic_auth: true
        validate_certs: "{{ aap_validate_certs }}"
        status_code: [204, 202]
      loop: "{{ dup_matrix }}"
